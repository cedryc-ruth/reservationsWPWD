{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>{{ titre }}</h1>

    <p>{{ artist }}</p>
    
    {% for type in artist.types  %}
        <li>{{ type }}</li>
    {% endfor %}
</div>
{% endblock %}
